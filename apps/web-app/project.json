{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "web-app",
  "projectType": "application",
  "sourceRoot": "apps/web-app/src",
  "implicitDependencies": ["server", "shared"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/web-app",
        "command": "pnpm dev"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/apps/web-app/dist"],
      "options": {
        "cwd": "apps/web-app",
        "command": "pnpm build"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/web-app",
        "command": "pnpm preview"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/web-app",
        "command": "pnpm start"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/web-app",
        "command": "pnpm typecheck"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/web-app",
        "command": "pnpm lint"
      }
    }
  }
}
