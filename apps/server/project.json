{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "server",
  "projectType": "application",
  "sourceRoot": "apps/server/src",
  "implicitDependencies": ["native-port-resolution"],
  "targets": {
    "build": {
      "outputs": ["{workspaceRoot}/apps/server/dist"]
    }
  }
}
