{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "server",
  "projectType": "application",
  "sourceRoot": "apps/server/src",
  "implicitDependencies": ["port-offset"],
  "targets": {
    "build": {
      "outputs": ["{workspaceRoot}/apps/server/dist"]
    }
  }
}
