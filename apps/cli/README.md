# @openkit/cli

Command-line interface for managing OpenKit projects, worktrees, and agent workflows. Handles project initialization, task resolution (Jira/Linear/local), agent coordination, and terminal management.

## Usage

```bash
openkit init          # Initialize a project
openkit task <ID>     # Work on a task
openkit start         # Start the server + UI
```
