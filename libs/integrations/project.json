{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "integrations",
  "projectType": "library",
  "sourceRoot": "libs/integrations/src",
  "implicitDependencies": ["shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsgo --noEmit -p tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec oxlint libs/integrations/src"
      }
    }
  }
}
