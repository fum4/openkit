{
  "name": "@openkit/integrations",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check:types": "tsgo --noEmit -p tsconfig.json",
    "check:lint": "oxlint src",
    "fix:lint": "oxlint --fix src"
  },
  "dependencies": {
    "@openkit/logger": "workspace:*",
    "@openkit/shared": "workspace:*"
  }
}
