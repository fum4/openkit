---
description: OpenKit task workflow — use OpenKit CLI when user mentions issue keys or wants to work on a ticket
alwaysApply: true
---

Use CLI-first workflow via `openkit task ... --init`.

{{WORKFLOW}}
