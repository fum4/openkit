{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "agents",
  "projectType": "library",
  "sourceRoot": "libs/agents/src",
  "implicitDependencies": ["shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsgo --noEmit -p libs/agents/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec oxlint libs/agents/src"
      }
    }
  }
}
