{
  "name": "@openkit/shared",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:types": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@openkit/logger": "workspace:*",
    "picocolors": "^1.1.1"
  }
}
