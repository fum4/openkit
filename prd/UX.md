# This document outlines user experience tasks & improvements

# ✅ Redesign landing page

The landing page should be redesigned to be more catchy and better express what the app does. It should not predominantly list the features of the app, but expose to the user the app's value

# Redesign workspace page (might not do it)

Split workspace page into two separate pages:

- Workspace - keeps all worktree-related functionality
- Issues - keeps all issues-related functionality

Both of these should have their design improved, especially the main views (right-side), which are currently cluttered and do not look well

Don't forget to separate/duplicate info banners, TBD

# ✅ Improve error handling

Revisit all error messages/banners and make them clear & pretty (maybe add toasts)

# Mobile responsiveness

Well, not necessarily mobile, but the app should be a bit more responsive with regards to screen sizes. Users might make it a bit smaller in order to run it alongside a simulator, for instance. We should accomodate this slightly.

- make the sidebar autohide on smaller viewports
- do the same for worktree details view buttons (collapse them into a "..." button, or smth)
- rethink issues details view layout
